import { StyleSheet } from "react-native-web";

export const s = StyleSheet.create({
  root: {
    flexDirection: "row",
    justifyContent: "space-evenly",
    flex: 1,
    alignItems: "center",
  },
});
